{
  "name": "video-viewer.us",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file video viewer (static, no build step). This package.json exists only for the CI harness tooling.",
  "scripts": {
    "test": "node test/harness.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
